spring.application.name=gateway

# Gateway port
server.port=8080

# Consul agent (Consul runs in a container on the host)
spring.cloud.consul.host=localhost
spring.cloud.consul.port=8500

# Enable discovery-based routing and explicit routes below
# spring.cloud.gateway.discovery.locator.enabled=true

# Routes: expose product and stock services through the gateway using load-balanced URIs
# spring.cloud.gateway.routes[0].id=product-service
# spring.cloud.gateway.routes[0].predicates[0]=Path=/products/**
# # Use literal URL for testing (bypass discovery/load-balancer)
# spring.cloud.gateway.routes[0].uri=lb://product-service

# spring.cloud.gateway.routes[1].id=stock-service
# spring.cloud.gateway.routes[1].predicates[0]=Path=/stock/**
# # Use literal URL for testing (bypass discovery/load-balancer)
# spring.cloud.gateway.routes[1].uri=lb://stock-service

# Management endpoints for health
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
